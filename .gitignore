node_modules/
dist/
.env
.env.local
*.pem
*.key

# Never committed: real customer data pulled in for local-only demos.
.local/

# L10 meeting state -- runtime data the service writes, not source.
l10-state.json
